import React from 'react';
import ThemeSwitcher from "@/components/mode/Switcher";

const Navbar = () => {
    return (
        <div>
            <ThemeSwitcher/>
        </div>
    );
};

export default Navbar;